//1. Проверить, попадает ли точка в область
#include <iostream>
using namespace std;
int main() {
	int x, y;
	cout << "Vvedite x and y: ";
	cin >> x >> y;
	if ((x - 1) * (x - 1) + y * y <= 1)
		cout << "Yes";
	else
		cout << "No";
	return 0;
}
